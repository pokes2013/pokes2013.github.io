# 甚好系统自定义公式



格式：表名,需要取得的字段名,表的主键,关联字段名

以下是2025.7.11系统重置后，自己加入成功的公式：



销售订单中加入项目号-成功

技巧是加入到存货编码中，不然显示会有延迟。

```
free1-> getColValue(doc_inventory, free1, id,inventoryId);
```

料号，成功

```
free2-> getColValue(doc_inventory, free2, id,inventoryId);
```

采购申请单，调用销售订单号

```
saleOrderBillNo-> getColValue(sal_saleorder, billno, id,inventoryId);
```









```

```

